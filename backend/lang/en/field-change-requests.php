<?php

return [
    // Labels of the protected fields (config/field-change-requests.php),
    // keyed by field name: the config `label` is a FRONTEND i18n key, which
    // the backend cannot resolve, so notifications translate from here.
    'fields' => [
        'source_id' => 'Source',
    ],
];
